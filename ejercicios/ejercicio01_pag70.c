/*
El programa dados dos valores enteros (a, b) determinar el valor de la división entera y el resto de la división entera de los mismos (a, b), sin usar los operadores de
división entera ó el de residuo.
Fecha: 11/03/2025
*/
#include <stdio.h>
int main(){
	int a, b;
	int division;
	int residuo;
	printf("Ingresa el valor de a y el de b: ");
	scanf("%d", &a);
	scanf("%d", &b);

	printf("%*d", a);
}
